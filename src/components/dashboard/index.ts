/**
 * Dashboard Components — barrel export
 *
 * Surviving market-context components:
 *   - MarketPulse: top bar with market indices
 *   - ArcGaugeRow: four arc gauges (used by MarketPage)
 */

export { default as MarketPulse } from "./MarketPulse";
export { default as ArcGaugeRow } from "./ArcGauge";
