name: Project Board Automation

on:
  issues:
    types: [labeled, closed, reopened]
  pull_request:
    types: [opened, closed]
  pull_request_review:
    types: [submitted]

# The default GITHUB_TOKEN cannot write to user-level Projects V2, and there is
# no "projects" permission scope for it (an invalid scope makes the whole
# workflow fail at startup validation). All board writes use PROJECT_PAT — a
# classic PAT with `project` + `repo` scope stored as a repo secret.
permissions:
  contents: read

jobs:
  sync-status:
    runs-on: ubuntu-latest
    env:
      PROJECT_ID: "PVT_kwHOCdGZYc4Bbcft"
      STATUS_FIELD_ID: "PVTSSF_lAHOCdGZYc4BbcftzhWM-EQ"
    steps:
      - name: Sync issue/PR status to project board
        env:
          GH_TOKEN: ${{ secrets.PROJECT_PAT }}
          OWNER: ${{ github.repository_owner }}
          # Event values pass through env (never interpolated directly into the
          # script) to avoid shell injection from untrusted issue/PR text.
          EVENT_NAME: ${{ github.event_name }}
          EVENT_ACTION: ${{ github.event.action }}
          LABEL_NAME: ${{ github.event.label.name }}
          EVT_ISSUE_NUMBER: ${{ github.event.issue.number }}
          PR_BODY: ${{ github.event.pull_request.body }}
          PR_MERGED: ${{ github.event.pull_request.merged }}
          REVIEW_STATE: ${{ github.event.review.state }}
        run: |
          set -euo pipefail

          # ── Status option IDs ──────────────────────────────────────
          BACKLOG="f75ad846"
          NEEDS_PLANNING="61e4505c"
          READY_FOR_CODING="47fc9ee4"
          IN_PROGRESS="8a244bab"
          PR_OPEN="778733cb"
          IN_REVIEW="df73e18b"
          CHANGES_REQUESTED="182692a0"
          HUMAN_APPROVAL="4bff2a14"
          DONE="98236657"

          # ── Determine issue number and target status ───────────────
          ISSUE_NUMBER=""
          TARGET_STATUS=""

          if [ "$EVENT_NAME" = "issues" ]; then
            ISSUE_NUMBER="$EVT_ISSUE_NUMBER"

            if [ "$EVENT_ACTION" = "labeled" ]; then
              case "$LABEL_NAME" in
                agent:needs-planning)      TARGET_STATUS="$NEEDS_PLANNING" ;;
                agent:ready-for-coding)    TARGET_STATUS="$READY_FOR_CODING" ;;
                agent:in-progress)         TARGET_STATUS="$IN_PROGRESS" ;;
                agent:pr-open)             TARGET_STATUS="$PR_OPEN" ;;
                agent:needs-review)        TARGET_STATUS="$IN_REVIEW" ;;
                agent:changes-requested)   TARGET_STATUS="$CHANGES_REQUESTED" ;;
                human:needs-approval)      TARGET_STATUS="$HUMAN_APPROVAL" ;;
                *) echo "Label '$LABEL_NAME' has no status mapping; skipping." ; exit 0 ;;
              esac
            elif [ "$EVENT_ACTION" = "closed" ]; then
              TARGET_STATUS="$DONE"
            elif [ "$EVENT_ACTION" = "reopened" ]; then
              TARGET_STATUS="$BACKLOG"
            fi

          elif [ "$EVENT_NAME" = "pull_request" ]; then
            # Extract "Closes #N" from PR body
            ISSUE_NUMBER=$(printf '%s' "$PR_BODY" | grep -oiE 'closes #([0-9]+)' | grep -oE '[0-9]+' | head -1 || true)

            if [ -z "$ISSUE_NUMBER" ]; then
              echo "No linked issue found in PR body; skipping."
              exit 0
            fi

            if [ "$EVENT_ACTION" = "opened" ]; then
              TARGET_STATUS="$PR_OPEN"
            elif [ "$EVENT_ACTION" = "closed" ] && [ "$PR_MERGED" = "true" ]; then
              TARGET_STATUS="$DONE"
            fi

          elif [ "$EVENT_NAME" = "pull_request_review" ]; then
            ISSUE_NUMBER=$(printf '%s' "$PR_BODY" | grep -oiE 'closes #([0-9]+)' | grep -oE '[0-9]+' | head -1 || true)

            if [ -z "$ISSUE_NUMBER" ]; then
              echo "No linked issue found in PR body; skipping."
              exit 0
            fi

            case "$REVIEW_STATE" in
              changes_requested) TARGET_STATUS="$CHANGES_REQUESTED" ;;
              approved)          TARGET_STATUS="$HUMAN_APPROVAL" ;;
              *) echo "Review state '$REVIEW_STATE' has no status mapping; skipping." ; exit 0 ;;
            esac
          fi

          if [ -z "$ISSUE_NUMBER" ] || [ -z "$TARGET_STATUS" ]; then
            echo "No issue number or target status determined; skipping."
            exit 0
          fi

          echo "Issue #$ISSUE_NUMBER → Status option $TARGET_STATUS"

          # ── Find the project item ID for this issue ────────────────
          ITEM_QUERY='
          query($org: String!) {
            user(login: $org) {
              projectV2(number: 1) {
                items(first: 100) {
                  nodes {
                    id
                    content {
                      ... on Issue { number }
                    }
                  }
                }
              }
            }
          }'

          ITEM_ID=$(gh api graphql -f query="$ITEM_QUERY" -f org="$OWNER" \
            --jq ".data.user.projectV2.items.nodes[] | select(.content.number == $ISSUE_NUMBER) | .id" 2>/dev/null || true)

          if [ -z "$ITEM_ID" ]; then
            echo "Issue #$ISSUE_NUMBER is not in the project; skipping."
            exit 0
          fi

          echo "Found project item: $ITEM_ID"

          # ── Update the Status field ────────────────────────────────
          UPDATE_MUTATION='
          mutation($project: ID!, $item: ID!, $field: ID!, $value: String!) {
            updateProjectV2ItemFieldValue(input: {
              projectId: $project
              itemId: $item
              fieldId: $field
              value: { singleSelectOptionId: $value }
            }) {
              projectV2Item { id }
            }
          }'

          gh api graphql \
            -f query="$UPDATE_MUTATION" \
            -f project="$PROJECT_ID" \
            -f item="$ITEM_ID" \
            -f field="$STATUS_FIELD_ID" \
            -f value="$TARGET_STATUS"

          echo "✓ Updated issue #$ISSUE_NUMBER to status option $TARGET_STATUS"
